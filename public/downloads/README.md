Drop the release APK here, named `planova.apk`.

The download button on the site links to `/downloads/planova.apk` — the
filename must match exactly, or update `APK_PATH` in
`src/lib/content/download.ts` if you name it differently.
